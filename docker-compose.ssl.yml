services:
  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/codearts-mcp-ssl.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/ssl:/etc/nginx/ssl:ro
  codearts-mcp:
    environment:
      - MCP_AUTH_COOKIE_SECURE=true
